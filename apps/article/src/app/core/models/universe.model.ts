export interface Universe {
  name: string;
  cover?: string;
  description?: string;
  franchise: string;
  serie?: string;
  isCanon: boolean;
  isRemake: boolean;
  isReboot: boolean;
  isOriginal: boolean;
}
